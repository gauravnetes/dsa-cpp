#include<bits/stdc++.h>
using namespace std; 

int main() {
    unordered_set<int>st;  // does not stores in sorted order. stores unique elemets

    // all operations works same except the lower_bound and upper_bound
}