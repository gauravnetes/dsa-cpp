#include<bits/stdc++.h>
using namespace std; 

int main() {
    // int 
    int x = 10; 

    // long
    long long  y = 32523452342435; 
    cout << y << endl; 

    // float
    float z = 5.6; 
    float a = 3;
    cout << "sum of z and a: " << z + a << endl; 

    // // string
    // string s1, s2, s3; 
    // cin >> s1 >> s2 >> s3;
    // cout << s1 << " " << s2 << " " << s3 << endl;

    // get everything together in string
    string str; 
    getline(cin, str); // take the entire line and put it in the string
    cout << str << endl; 

    // char
    char cha = 'a'; 
    char ch; 
    cin >> ch; 
    cout << cha << ch; 
}